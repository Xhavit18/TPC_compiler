/*#include <stdio.h>
#include "math.h"*/

int r2, r1;
int max;
char salut;

int valeur(int a, int b, int c, int x) {
	reade(a);  
	a = b;
	print(a);
	
	while((a||b)&&a)
		b=2;
	return a+b;
}

int main(int argc){
	return 0;
}
